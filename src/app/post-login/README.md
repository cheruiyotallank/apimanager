# Post-Login Module Directory

This directory is designated for all authenticated application features and pages (e.g. Dashboard, API Catalog, Endpoint Manager, Analytics, Settings).

Features placed here require the user to be logged in with a valid session or JWT token.
